kind: Config
fields:
  name: Global
  content:
    - kind: Markdown
      fields:
        content: This is available from any document
    - kind: ConfigDefinition
      fields:
        params:
          language: yaml
        config: |
          paths:
            client: containers/www/client